const ID = 'id';
const NOW = 'NOW';
const CASCADE = 'CASCADE';
const SET_NULL = 'SET NULL';
const POSTGRES = 'postgres';
const UUID_V4_GENERATE = 'uuid_generate_v4';
const UUID_SUPPORT = 'CREATE EXTENSION IF NOT EXISTS "uuid-ossp";';

module.exports = {
  POSTGRES,
  UUID_SUPPORT,
  UUID_V4_GENERATE,
  NOW,
  CASCADE,
  SET_NULL,
  ID,
};
